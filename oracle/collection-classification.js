import crypto from 'node:crypto';
const token=v=>String(v||'').normalize('NFKC').trim().toLowerCase().replace(/[^a-z0-9]+/g,'_').replace(/^_|_$/g,'');
export function collectionClassificationDdl(project){return `CREATE TABLE IF NOT EXISTS \`${project}.commerce.collection_classifications\` (classification_id STRING NOT NULL,platform STRING NOT NULL,collection_id STRING NOT NULL,collection_title STRING,classification_type STRING NOT NULL,classification_value STRING NOT NULL,status STRING NOT NULL,provenance STRING NOT NULL,reviewed_by STRING NOT NULL,reviewed_at TIMESTAMP NOT NULL,note STRING,supersedes_classification_id STRING,revoked_at TIMESTAMP) CLUSTER BY platform,collection_id,status`}
export function validateCollectionDecision(input,reviewedBy,now=new Date().toISOString()){const type=input.classification_type,value=token(input.classification_value);if(input.platform!=='shopify'||!/^[0-9]+$/.test(String(input.collection_id||'')))throw new Error('invalid Shopify collection identity');if(!['collection_group','collaboration_name'].includes(type))throw new Error('invalid collection classification type');if(!value)throw new Error('classification value is required');if(type==='collection_group'&&!['collaboration','other'].includes(value))throw new Error('collection_group must be collaboration or other');const basis=[input.platform,input.collection_id,type,value,now,reviewedBy].join('|');return {...input,classification_id:`cc_${crypto.createHash('sha256').update(basis).digest('hex').slice(0,32)}`,classification_value:value,status:'active',provenance:'human_governed',reviewed_by:reviewedBy,reviewed_at:now,note:input.note||null,supersedes_classification_id:input.supersedes_classification_id||null,revoked_at:null}}
export function createCollectionClassificationService({bigquery,project}){const table=`${project}.commerce.collection_classifications`;return {
  async setup(){await bigquery.query({query:collectionClassificationDdl(project)})},
  async list({search='',status='',sort='product_count_desc'}={}){const [rows]=await bigquery.query({query:`WITH active AS (SELECT * EXCEPT(rn) FROM (SELECT *,ROW_NUMBER() OVER(PARTITION BY platform,collection_id,classification_type ORDER BY reviewed_at DESC,classification_id DESC) rn FROM \`${table}\`) WHERE rn=1 AND status='active') SELECT c.collection_id,c.title,c.handle,c.product_count,ARRAY_AGG(STRUCT(a.classification_id,a.classification_type,a.classification_value,a.reviewed_by,a.reviewed_at) IGNORE NULLS) classifications FROM \`${project}.shopify_catalogue.collections\` c LEFT JOIN active a USING(collection_id) GROUP BY c.collection_id,c.title,c.handle,c.product_count`});
    const decorated=rows.map(row=>{const group=(row.classifications||[]).find(x=>x.classification_type==='collection_group'),name=(row.classifications||[]).find(x=>x.classification_type==='collaboration_name');return {...row,governance_status:group?.classification_value||'unclassified',collaboration_name:name?.classification_value||null}});
    const filtered=decorated.filter(row=>(!search||`${row.title||''} ${row.handle||''} ${row.collection_id}`.toLowerCase().includes(search.toLowerCase()))&&(!status||row.governance_status===status));
    filtered.sort(sort==='title_asc'?(a,b)=>String(a.title).localeCompare(String(b.title)):(a,b)=>Number(b.product_count||0)-Number(a.product_count||0)||String(a.title).localeCompare(String(b.title)));
    const count=value=>decorated.filter(x=>x.governance_status===value).length;return {collections:filtered,summary:{total:decorated.length,collaboration:count('collaboration'),other:count('other'),unclassified:count('unclassified'),conflicts:count('conflict')}}},
  async detail(collectionId){const [collections]=await bigquery.query({query:`SELECT collection_id,title,handle,product_count FROM \`${project}.shopify_catalogue.collections\` WHERE collection_id=@collection_id`,params:{collection_id:String(collectionId)}});if(!collections[0])throw new Error('collection not found');const [products]=await bigquery.query({query:`SELECT p.product_id,p.title FROM \`${project}.shopify_catalogue.product_collections\` pc JOIN \`${project}.shopify_catalogue.products\` p USING(product_id) WHERE pc.collection_id=@collection_id ORDER BY p.title LIMIT 50`,params:{collection_id:String(collectionId)}});const [history]=await bigquery.query({query:`SELECT * FROM \`${table}\` WHERE platform='shopify' AND collection_id=@collection_id ORDER BY reviewed_at DESC,classification_id DESC`,params:{collection_id:String(collectionId)}});return {collection:collections[0],products,history}},
  async decide(input,user){await this.setup();const clean={platform:input.platform,collection_id:String(input.collection_id),collection_title:input.collection_title||null,classification_type:input.classification_type,classification_value:input.classification_value,note:input.note||null};const [previous]=await bigquery.query({query:`SELECT classification_id FROM \`${table}\` WHERE platform=@platform AND collection_id=@collection_id AND classification_type=@classification_type AND status='active' ORDER BY reviewed_at DESC LIMIT 1`,params:clean});const row=validateCollectionDecision({...clean,supersedes_classification_id:previous[0]?.classification_id||null},user);await bigquery.query({query:`UPDATE \`${table}\` SET status='superseded' WHERE platform=@platform AND collection_id=@collection_id AND classification_type=@classification_type AND status='active'; INSERT \`${table}\` SELECT r.* FROM UNNEST(@rows) r`,params:{...row,rows:[row]}});return row},
  async classify(input,user){if(!['collaboration','other'].includes(input.collection_group))throw new Error('collection_group must be collaboration or other');if(input.collection_group==='collaboration'&&!String(input.collaboration_name||'').trim())throw new Error('collaboration_name is required');const base={platform:'shopify',collection_id:input.collection_id,collection_title:input.collection_title,note:input.note};const group=await this.decide({...base,classification_type:'collection_group',classification_value:input.collection_group},user);let name=null;if(input.collection_group==='collaboration')name=await this.decide({...base,classification_type:'collaboration_name',classification_value:input.collaboration_name},user);else{const [active]=await bigquery.query({query:`SELECT classification_id FROM \`${table}\` WHERE platform='shopify' AND collection_id=@collection_id AND classification_type='collaboration_name' AND status='active'`,params:{collection_id:String(input.collection_id)}});for(const row of active)await this.revoke({classification_id:row.classification_id},user)}return {group,name}},
  async revoke(input,user){await bigquery.query({query:`UPDATE \`${table}\` SET status='revoked',revoked_at=CURRENT_TIMESTAMP(),reviewed_by=@user WHERE classification_id=@id AND status='active'`,params:{id:input.classification_id,user}});return {revoked:true}}
}}
